# Docker

Contiene el archivo maestro `docker-compose.yml` y las configuraciones necesarias para levantar toda la infraestructura del Data Lakehouse: **MinIO** (object storage), **Trino** (query engine), **Hive Metastore** y **Apache Airflow**.
